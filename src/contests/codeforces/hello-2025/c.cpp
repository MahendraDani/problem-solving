#include <iostream>
#include <bits/stdc++.h>

#define ll long long
#define vi vector<int>
#define pi pair<int,int>
#define vll vector<long long>

using namespace std;

/*
 How to chose a,b,c from l to r to max => (a^b) + (b^c) + (a^c)
 Iteratin over will TLE given the constraints on l and r
 */
void solve(){
  ;
}

int main(){
  int t;
  cin >> t;
  while(t--){
    solve();
  }
  return 0;
}
